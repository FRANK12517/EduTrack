#!/usr/bin/env node
'use strict';
const fs=require('node:fs');const source=process.env.EDUTRACK_MIGRATION_SOURCE||process.argv[2];if(!source||!fs.existsSync(source)){console.log(JSON.stringify({status:'NOT_PROVEN',reason:'PRODUCTION EXPORT UNAVAILABLE',sourceProvided:false},null,2));process.exit(2)}let data;try{data=JSON.parse(fs.readFileSync(source,'utf8'))}catch(error){console.log(JSON.stringify({status:'FAIL',reason:'Migration source is not valid JSON',message:error.message},null,2));process.exit(1)}const collections=['paymentIntents','transactions','subscriptions','paymentEvents','files','passwordResets','audit'];const discovered=Object.fromEntries(collections.map(k=>[k,Array.isArray(data[k])?data[k].length:0]));console.log(JSON.stringify({status:'PASS',sourceProvided:true,sourcePath:'[configured]',discovered,manualReview:[],originalSourceMutated:false,note:'Run npm run db:migrate-business against this copied source and reconcile its generated manualReview before release.'},null,2));
