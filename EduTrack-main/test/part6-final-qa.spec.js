const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const root=path.join(__dirname,'..');const html=fs.readFileSync(path.join(root,'index.html'),'utf8');const server=fs.readFileSync(path.join(root,'server.js'),'utf8');const polish=fs.readFileSync(path.join(root,'edutrack-polish.css'),'utf8');const dense=fs.readFileSync(path.join(root,'edutrack-dense.css'),'utf8');const shell=fs.readFileSync(path.join(root,'edutrack-shell.css'),'utf8');for(const marker of ['edutrack-design-system.css','edutrack-shell.css','edutrack-dashboard.css','edutrack-dense.css','edutrack-polish.css'])assert.match(html,new RegExp(marker.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));for(const marker of ['SCHOOL','DISTRICT','REGIONAL','NATIONAL','PARENT','STUDENT','online-admission','score-entry','broadsheet','attendance','communication','chat','control-panel','analytics'])assert.match(html,new RegExp(marker,'i'));for(const marker of ['html{max-width:100%;overflow-x:hidden}','body{max-width:100%;overflow-x:hidden}','z-index:4000','z-index:5000','pointer-events:none','pointer-events:auto','touch-action:manipulation','@media(max-width:480px)'])assert.match(polish,new RegExp(marker.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));for(const marker of ['score-table','attend-table','broadsheet-table','-webkit-overflow-scrolling:touch','min-width:44rem'])assert.match(dense,new RegExp(marker.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));for(const marker of ['sidebar','topbar','nav-item','@media(max-width:700px)'])assert.match(shell,new RegExp(marker.replace(/[.*+?^${}()|[\]\\]/g,'\\$&'),'i'));assert.match(server,/SAFE_PUBLIC_FILES/);assert.match(server,/edutrack-polish\.css/);assert.doesNotMatch(polish,/animate\.css|framer-motion|gsap|jquery/);console.log('Part 6 final QA regression suite passed.');
