Part 41 function diagnostic

Target deployment: dpl_GcnWAdJnF9xgBWbMBa7PiidnkMvo
Target branch: part41-staging

Part 40 runtime evidence:
The Vercel serverless runtime failed during module import at server.js ensureData(). The exact error was ENOENT while attempting mkdir '/var/task/data'. The Vercel function filesystem is not a writable application data directory, and the module-level JSON initialization ran before the handler could execute.

Correction implemented:
server.js now detects Vercel/AWS Lambda serverless execution and skips JSON filesystem initialization during production import. Production still executes assertProductionConfiguration(), which requires relational persistence and exact HTTPS configuration. No JSON fallback is introduced for production.

Post-repair local diagnostic:
The filesystem error no longer occurs. With production serverless mode and no database URL, import fails closed with the safe message that EDUTRACK_DATABASE_URL is required. This is expected and confirms that the next remaining blocker is missing staging configuration, not filesystem initialization.

Part 41 preview runtime evidence:
The project metadata still reports Node.js 24.x. Real preview requests after the filesystem repair returned HTTP 500 FUNCTION_INVOCATION_FAILED, and Vercel runtime logs reported: Production requires exact HTTPS EDUTRACK_ALLOWED_ORIGINS without wildcards. The staging project has no verified relational/database, Paystack, S3, or exact-origin environment configuration.

Secret handling:
No secret values, cookies, access tokens, passwords, database credentials, or provider keys are recorded here.
