# Part 42 Operator Handoff

Part 42 is **BLOCKED**. The repository-side Part 41 repair remains intact, but the authorized connected infrastructure cannot provide the complete isolated staging environment required for real backend validation. The connected Vercel project reports Node.js 24.x; no authorized environment-variable management operation is available; and no isolated staging database, private S3 target, Paystack sandbox credentials, delivery sink, monitoring target, or backup destination was available.

The safe next action is to provision an isolated non-production target that runs Node.js 22 and configure only staging values through the approved secret mechanism. Required names discovered from source are `EDUTRACK_DATABASE_URL`, `EDUTRACK_ALLOWED_ORIGINS`, `PAYSTACK_SECRET_KEY`, `PAYSTACK_WEBHOOK_SECRET`, `EDUTRACK_PAYMENT_PLANS`, `EDUTRACK_STORAGE_MODE`, `EDUTRACK_STORAGE_BUCKET`, `EDUTRACK_STORAGE_REGION`, `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `EDUTRACK_RESET_DELIVERY_PROVIDER`, `EDUTRACK_BACKUP_DESTINATION`, `EDUTRACK_BACKUP_ENCRYPTION_KEY_FILE`, and `EDUTRACK_MONITORING_URL`. Values must never be placed in Git, reports, logs, or test output.

```text
Provision isolated Node.js 22 staging resources only. Configure a dedicated relational database, exact HTTPS EDUTRACK_ALLOWED_ORIGINS, private S3 bucket/prefix, Paystack sandbox credentials, non-production delivery sink, monitoring target, and staging backup destination. Deploy the exact Part 41 source SHA 63b6739b72389d066d496a9b1e12afad13137cec plus reviewed Part 42 changes as a clean immutable release. Verify the deployed SHA and actual runtime. Run GET and OPTIONS checks for /api/health and /api/auth/session, approved and unauthorized CORS, application security headers, database migration/schema/persistence, private storage, sandbox payment, authentication, RBAC, tenant isolation, mobile, performance, monitoring, backup/restore, and rollback. Record only actual results. Never use production resources, live Paystack, real users, production data, production storage, or production alerting. Keep Part 42 BLOCKED until every mandatory staging gate is independently proven. Do not start Part 43.
```

Do not disable SSO, weaken guards, change CORS to wildcard, use production resources, or promote the preview. Part 43 must not begin until the staging blockers are resolved and independently evidenced.
