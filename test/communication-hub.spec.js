const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const root=path.join(__dirname,'..');const rel=fs.readFileSync(path.join(root,'db','relational.js'),'utf8');const server=fs.readFileSync(path.join(root,'server.js'),'utf8');const ui=fs.readFileSync(path.join(root,'communication-hub.js'),'utf8');const html=fs.readFileSync(path.join(root,'index.html'),'utf8');for(const x of ['communication_campaigns','communication_campaign_recipients','resolveCommunicationAudience','createCommunicationCampaign','listCommunicationCampaigns','getCommunicationCampaign','recipient_count','schedule_at','SCHEDULED','SENT','PENDING'])assert.match(rel,new RegExp(x));for(const x of ['/api/communications/audience-count','/api/communications/campaigns','communications.manage','communications.view'])assert.match(server,new RegExp(x.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));for(const x of ['Communication Hub','SMS','EMAIL','Send now','Schedule campaign','Message preview','Campaign History','recipientCount','confirm','disabled'])assert.match(ui,new RegExp(x.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));assert.match(html,/communication-hub\.js/);for(const level of ['NATIONAL','PARENT','DISTRICT','REGIONAL','SCHOOL','STUDENT'])assert.match(html,new RegExp(level));assert.match(html,/smslogs/);console.log('Communication Hub regression suite passed.');
