const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const root=path.join(__dirname,'..');const ui=fs.readFileSync(path.join(root,'online-admission.js'),'utf8');const server=fs.readFileSync(path.join(root,'server.js'),'utf8');const rel=fs.readFileSync(path.join(root,'db','relational.js'),'utf8');const schema=fs.readFileSync(path.join(root,'db','schema.sql'),'utf8');for(const x of ['Applicant full name','Date of birth','Guardian / parent name','Guardian phone','Guardian email','SUBMIT ADMISSION APPLICATION','Change Selection','APPLICATION SUBMITTED SUCCESSFULLY'])assert.match(ui,new RegExp(x.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));for(const x of ['Previous school','Previous class','Reason for transfer'])assert.match(ui,new RegExp(x.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));assert.match(ui,/edutrack_online_admission_context/);assert.match(ui,/No Permanent Student ID or Index Number was generated/);assert.match(ui,/btn\.disabled=true/);assert.match(ui,/api\('\/api\/admissions\/applications'/);assert.match(server,/\/api\/admissions\/applications/);assert.match(rel,/submitPendingAdmission/);assert.match(rel,/PENDING_REVIEW/);assert.match(rel,/pending_admission_applications/);assert.match(rel,/applicant_hash/);assert.match(rel,/s\.active=TRUE/);assert.match(rel,/c\.school_id=s\.id/);assert.match(schema,/CREATE TABLE IF NOT EXISTS pending_admission_applications/);assert.doesNotMatch(ui,/createStudent|PermanentIdAuthorization|student_identifier/);console.log('Online Admissions Part 2 regression suite passed.');
