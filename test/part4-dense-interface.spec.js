const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const root=path.join(__dirname,'..');const css=fs.readFileSync(path.join(root,'edutrack-dense.css'),'utf8');const html=fs.readFileSync(path.join(root,'index.html'),'utf8');const server=fs.readFileSync(path.join(root,'server.js'),'utf8');for(const marker of ['.form-grid','.form-group','.filter-bar','.search-bar','.tbl-wrap','.table-wrap','.score-table','.attend-table','.broadsheet-table','.modal-overlay','.modal-close','.tabs','.pagination','min-width:44rem','-webkit-overflow-scrolling:touch','max-width:700px','prefers-reduced-motion'])assert.match(css,new RegExp(marker.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));for(const marker of ['score-table','attend-table-wrap','broadsheet','form-grid','form-group','modal-overlay','filter-bar','search-bar','tbl-wrap'])assert.match(html,new RegExp(marker.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));for(const marker of ['edutrack-design-system.css','edutrack-shell.css','edutrack-dashboard.css','edutrack-dense.css'])assert.match(html,new RegExp(marker.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));assert.match(server,/edutrack-dense\.css/);for(const label of ['NATIONAL','PARENT','DISTRICT','REGIONAL','SCHOOL','STUDENT'])assert.match(html,new RegExp(label));console.log('Part 4 dense-interface regression suite passed.');
