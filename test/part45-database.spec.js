'use strict'; if(!process.env.EDUTRACK_DATABASE_URL){console.log('Part 45 database: NOT_PROVEN (isolated relational staging database unavailable).');process.exit(0)}; if(!/^mysql:|^mariadb:/.test(process.env.EDUTRACK_DATABASE_URL))throw Error('Unsupported database.');
