'use strict'; const required=['EDUTRACK_DATABASE_URL','EDUTRACK_ALLOWED_ORIGINS','PAYSTACK_SECRET_KEY','PAYSTACK_WEBHOOK_SECRET','EDUTRACK_STORAGE_BUCKET','AWS_ACCESS_KEY_ID','AWS_SECRET_ACCESS_KEY']; const missing=required.filter(x=>!process.env[x]); if(missing.length)console.log(`Part 47 environment: BLOCKED (${missing.length} staging values unavailable; values omitted).`); if(process.env.EDUTRACK_ALLOWED_ORIGINS==='*')throw Error('Wildcard CORS forbidden.');
