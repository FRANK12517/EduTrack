const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const root=path.join(__dirname,'..');const js=fs.readFileSync(path.join(root,'quiz-module.js'),'utf8');const rel=fs.readFileSync(path.join(root,'db','relational.js'),'utf8');const srv=fs.readFileSync(path.join(root,'server.js'),'utf8');const html=fs.readFileSync(path.join(root,'index.html'),'utf8');for(const marker of ['type="radio"','name="quiz-question-','answers[q.id]','/api/quizzes/submit','Submit Quiz','Grading','percentage','saved to the gradebook','dataset.quizId','setInterval'])assert.match(js,new RegExp(marker.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));for(const marker of ['QUIZ_TABLES','quizzes','quiz_questions','quiz_attempts','quiz_responses','submitStudentQuiz','getStudentQuiz','SCHEMA_VERSION = 18'])assert.match(rel,new RegExp(marker.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));for(const marker of ['/api/quizzes/','quizId','submitStudentQuiz'])assert.match(srv,new RegExp(marker.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));assert.match(srv,/roles:\s*\[\s*['\"]STUDENT/);assert.match(html,/quiz-module\.js/);for(const label of ['SCHOOL','DISTRICT','REGIONAL','NATIONAL','PARENT','STUDENT'])assert.match(html,new RegExp(label));console.log('Quiz radio-button and instant auto-grading regression suite passed.');
