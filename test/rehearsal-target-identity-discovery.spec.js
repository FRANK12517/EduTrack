'use strict';const assert=require('node:assert/strict');const {config}=require('../scripts/rehearsal-target-identity-discovery');const env={EDUTRACK_TIDB_RECOVERY_REHEARSAL_DATABASE_URL:'mysql://hidden:hidden@example.test/db',EDUTRACK_RECOVERY_REHEARSAL_TARGET:'isolated-recovery-rehearsal'};assert.equal(config(env).multipleStatements,false);assert.equal(config(env).ssl.rejectUnauthorized,true);assert.throws(()=>config({}));assert.throws(()=>config({...env,EDUTRACK_RECOVERY_REHEARSAL_TARGET:'production'}));console.log('Rehearsal identity discovery guard passed.');
